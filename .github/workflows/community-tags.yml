name: Sync community tags and publish
on:
  issues:
    types: [opened, edited, closed, reopened, deleted, locked, unlocked]
  workflow_dispatch:
permissions:
  contents: write
  issues: read
  pages: write
  id-token: write
concurrency:
  group: skills-publication
  cancel-in-progress: false
jobs:
  build:
    outputs:
      data-sha: ${{ steps.data-digest.outputs.sha }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - run: node scripts/community-tags.mjs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: node scripts/validate-data.mjs
      - run: npm run build
      - name: Save community annotations
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/community-tags.json
          if ! git diff --cached --quiet; then
            git commit -m 'Update community tags'
            git push
          fi
      - name: Record published data checksum
        id: data-digest
        run: echo "sha=$(sha256sum dist/assets/data.json | cut -d ' ' -f1)" >> "$GITHUB_OUTPUT"
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
      - name: Verify live data matches artifact
        env:
          EXPECTED_SHA: ${{ needs.build.outputs.data-sha }}
        run: |
          for attempt in 1 2 3 4 5 6; do
            if curl --fail --silent --show-error --max-time 20 "https://xylopyrifer.github.io/github-skills-weekly/assets/data.json?verify=$GITHUB_RUN_ID" -o "$RUNNER_TEMP/skills-live.json"; then
              ACTUAL_SHA=$(sha256sum "$RUNNER_TEMP/skills-live.json" | cut -d ' ' -f1)
              if [ "$ACTUAL_SHA" = "$EXPECTED_SHA" ]; then
                echo 'Live data checksum matches the validated artifact.'
                exit 0
              fi
            fi
            sleep 10
          done
          echo 'Live data does not match the validated artifact.'
          exit 1
