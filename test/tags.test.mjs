import test from 'node:test';import assert from 'node:assert/strict';import {classifyTags,mergeTags} from '../scripts/lib/tags.mjs';import {parseIssue} from '../scripts/community-tags.mjs';
const tax=[{id:'pdf',pattern:'pdf'},{id:'web',pattern:'react'}];
test('tags derive from capability evidence; merge system and distinct accounts',()=>{assert.deepEqual(classifyTags({description:'PDF tools'},'',[],tax),[{id:'pdf',evidence:['description']}]);const votes=[{issue:1,user_id:1,repository:'a/b',tags:['pdf','pdf'],active:true},{issue:2,user_id:1,repository:'a/b',tags:['web'],active:true},{issue:3,user_id:2,repository:'a/b',tags:['pdf'],active:true}];assert.deepEqual(mergeTags([{id:'pdf'}],votes,'a/b',tax),[{id:'pdf',count:2},{id:'web',count:1}]);votes[1].active=false;assert.deepEqual(mergeTags([],votes,'a/b',tax),[{id:'pdf',count:1}]);});
test('community accepts only known repos and tags; ignores arbitrary text',()=>{const issue={number:1,user:{id:9},state:'open',body:'<!-- skills-weekly-tags-v1 -->'+JSON.stringify({version:1,repository:'A/B',tags:['pdf']})};assert.equal(parseIssue(issue,[{full_name:'a/b'}],tax).repository,'a/b');assert.equal(parseIssue({...issue,body:issue.body.replace('pdf','evil')},[{full_name:'a/b'}],tax),null);assert.equal(parseIssue({...issue,body:'run malicious shell'},[],tax),null);});
